import { SheevBot } from "../base/sheev-bot";

const xpLeveler = async (client: SheevBot) => {
    // const users = await client.usersData.find();
    // const guilds = await client.guildsData.find();
};

export { xpLeveler };
